# Never boot an image whose security counter/version is older than the
# highest successfully booted image. This is enforced by MCUboot itself.
CONFIG_MCUBOOT_DOWNGRADE_PREVENTION=y
CONFIG_BOOT_IMG_HASH_ALG_SHA256=y
